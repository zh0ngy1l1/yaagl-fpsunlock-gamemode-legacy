# Unified diff context includes space-only lines and may end with blank context.
# Keep the exported patch byte-identical; check source whitespace in the Wine repo.
allow-fixed-size-fullscreen.patch whitespace=-blank-at-eol,-blank-at-eof
